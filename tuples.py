
#tuples is familiar to list
#tuple we can't change and modify (tuple is immutable)
#the difference between a list and a tuple is brackets.
#we can make tuples in list
coordinates = [(4, 5), (9, 12), (32, 15)]
print(coordinates[0])